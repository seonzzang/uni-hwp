#!/bin/sh

repo_root="$(git rev-parse --show-toplevel)"
powershell -NoProfile -ExecutionPolicy Bypass -File "$repo_root/tools/version-agent.ps1"
status=$?

if [ $status -ne 0 ]; then
  exit $status
fi

exit 0
